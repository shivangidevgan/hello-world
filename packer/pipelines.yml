pool:
  vmImage: ubuntu-latest

steps:
- task: PackerTool@0
  inputs:
    version: '1.*'

- task: PackerBuild@1
  inputs:
    templateType: 'custom'
    customTemplateLocation: '$(Build.SourcesDirectory)/hello-world/packer/windows.json'